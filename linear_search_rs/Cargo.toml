[package]
name = "linear_search_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "linear_search_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
numpy = "0.20"

[package.metadata.maturin]
name = "linear_search"
